import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#726E8D',
            dark: '#2A254B',
            light: '#83A9AC',
            contrastText: '#ffffff'
        }
    }
})